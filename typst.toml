[package]
name = "fancy-units"
version = "0.2.0"
entrypoint = "src/lib.typ"
authors = ["Janek Fleper <@janekfleper>"]
license = "MIT"
compiler = "0.14.0"
repository = "https://github.com/janekfleper/typst-fancy-units"
description = "Format numbers and units with style."
keywords = ["numbers", "units"]
categories = ["visualization"]
disciplines = ["chemistry", "engineering", "physics"]
exclude = ["docs", "tests"]
